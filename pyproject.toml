[tool.ruff]
target-version = "py310"
line-length = 120
extend-exclude = [
  ".venv",
  "pretrained_models",
  "results",
  "hugging_face/test_sample",
]

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E402", "E501"]
